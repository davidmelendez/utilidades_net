

namespace SUN.FactoryDB
{
    public enum  TiposBaseDatos
    {
        SQLServer,
        Oracle,
		MySQL
    }

    public enum TiposDatos
    {
        Int,
        String,
        DateTime,
        Decimal,
        Cursor,
        Clob
    }
}
